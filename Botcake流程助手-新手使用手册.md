# Botcake 流程助手新手使用手册

这款插件用于把已经配置好的 Botcake 专页设置和流程做成可重复使用的资源。以后更换专页时，不必再逐个创建节点、填写评论回复或重新建立机器人变量。

最常用的操作只有三步：

1. 在 Botcake 中进入要设置的专页。
2. 打开右侧的“专页助手”，选择一个设置或流程。
3. 按提示填写变量，然后点击应用。

## 一、首次安装和准备

### 1. 加载插件

1. 打开 Chrome，在地址栏输入 `chrome://extensions/`。
2. 打开右上角的“开发者模式”。
3. 将本插件的 `dist` 文件夹拖拽到浏览器上进行安装。
4. 建议把“Botcake 流程助手”固定到浏览器工具栏。

### 2. 登录 Botcake

插件使用当前浏览器中已经登录的 Botcake 账号，不需要另外填写 Token。请先正常登录 Botcake，并确认账号拥有目标专页的操作权限。

### 3. 设置资源控制台表格

点击浏览器工具栏中的插件图标，在“资源控制台表格”中粘贴公开的 Google 表格链接。链接必须带有 `gid`。

表格只需要两列：

| 第一列：名称 | 第二列：公开资源链接 |
| --- | --- |
| 设置-默认设置 | 设置 JSON 的公开链接 |
| 流程-流程1号 | 流程 ZIP 的公开链接 |
| 默认回复-1号 | 流程 ZIP 的公开链接 |

名称开头决定资源出现在哪个区域：

- `设置`：专页设置包。
- `流程`：评论私信流程和普通 Flow 模板。
- `默认回复`：Botcake 系统默认回复模板。

表格可以有表头，也可以直接从第一行填写资源。Google 表格和 Google Drive 文件都要设置为“知道链接的任何人可查看”。

控制台正常情况下最多每 5 分钟自动重新读取一次，避免频繁请求 Google。修改表格后如需立即生效，可在插件图标中点击“重新读取”。网络异常时最多自动重试 1 次，并尽量继续使用上次成功读取的目录。

## 二、认识两个助手界面

### 专页助手

进入 Botcake 的某个专页后，页面右侧会出现“专页助手”浮标。浮标和面板都可以拖动。

专页助手用于：

- 导出或应用专页设置。
- 应用评论私信流程。
- 把评论私信流程同步为欢迎信息流程。
- 应用并开启默认回复流程。
- 从电脑加载设置 JSON 或流程 ZIP。

### 流程助手

进入普通 Flow 编辑页或默认回复编辑页后，浮标会自动变成“流程助手”。

流程助手用于：

- 把控制台中的模板应用到当前 Flow。
- 从电脑加载流程 ZIP。
- 导出当前流程模板。
- 备份和恢复当前流程。

普通 Flow 页面只显示名称以“流程”开头的资源；默认回复页面只显示名称以“默认回复”开头的资源，避免选错类型。

## 三、一键应用专页设置

在专页助手的“专页设置”区域，点击某个资源右侧的“应用设置”。插件会自动设置该 JSON 中包含的内容。

目前设置包包括：

- 时区。
- 目标国家或地区。
- 评论自动回复及相关开关。
- 一级评论回复文案。
- 机器人变量。

评论私信、欢迎信息和默认回复流程不放在设置包中，需要在各自的流程区域单独应用。

机器人变量按名称处理：

- 目标专页已有同名变量时直接复用。
- 同名变量已归档时自动恢复，并保留原来的变量 ID。
- 完全不存在时自动创建。

完成后，顶部提示会显示更新了多少项、新建了多少变量、恢复了多少归档变量。

## 四、应用评论私信流程

1. 进入目标专页并打开专页助手。
2. 在“评论私信流程”区域选择模板，点击“应用评论私信”。
3. 如果模板有变量，会进入填写页面。
4. 填完必填项后点击底部的“应用评论私信”。

如果专页已经有评论私信流程，插件会替换原流程；如果没有，会自动创建名为“评论”的流程，并开启 Auto-inbox。

“同步欢迎信息流程”默认开启。开启时，评论私信流程应用成功后，同一个流程还会被设为欢迎信息流程并启用。若不需要，请在应用前取消勾选。

自动应用过程中插件会进入目标 Flow 页面，这是正常现象。任务中保存了目标专页和 Flow ID，即使中途误跳到其他 Flow，也不会覆盖错误的流程。

## 五、应用默认回复流程

在专页助手的“默认回复流程”区域点击“应用默认回复”。填写变量后，插件会：

1. 复用 Botcake 当前的系统默认回复；没有时才创建。
2. 替换默认回复中的节点和内容。
3. 自动开启 `Activate`。
4. 切换到普通 `Default` 模式，关闭 AI 默认回复模式。

因此重复应用不会新增默认回复，而是在同一个系统默认回复上更新。

评论私信模板和默认回复模板使用相同的流程 ZIP 格式。如果同一个模板两处都要使用，可在控制台中分别建立“流程-…”和“默认回复-…”两行，并填写同一个 ZIP 链接。

## 六、填写流程变量

模板可以把经常变化的文案、数字、链接、图片、音频和视频做成变量。

### 文字和数字

- 按输入框上方的名称和说明填写。
- 带红色星号的是必填项。
- 同一个变量出现在多个节点或多个链接中时，只需填写一次，所有位置会同步替换。
- 变量可以放在固定内容中间，例如网址前半部分固定，只替换其中的账号 ID。

### 随机选项

随机变量可以预先设置多项内容。用户不选择时，插件会随机使用其中一项；手动选择后则使用指定项。同一个变量的所有位置始终使用同一次结果。

### 图片、音频和视频

素材变量可使用：

- 模板 ZIP 内置素材。
- 模板作者预设的公开 URL 或 Google Drive 文件。
- 用户从电脑临时选择的文件。

同一个素材变量可以绑定多个位置，只需选择一次。远程素材必须公开可下载，单个素材不能超过 30MB。

## 七、制作专页设置包

1. 选择一个已经手动配置正确的 Botcake 专页。
2. 打开专页助手。
3. 点击顶部的“导出专页设置 JSON”。
4. 把下载的 JSON 上传到公开的 Google Drive 或其他 HTTPS 网盘。
5. 在控制台表格新增一行，名称以“设置”开头，第二列填写公开文件链接。

导出的评论回复只保留一级回复。本插件当前不处理手机号、提及和直播评论流程。

## 八、制作流程模板 ZIP

### 直接导出

1. 打开已经制作好的 Botcake Flow。
2. 打开流程助手右上角的三点菜单。
3. 点击“导出当前流程模板”。

插件会打包入口节点、全部节点和连线、机器人变量依赖，以及能够下载到的图片、音频和视频。

### 使用模板编辑器整理

点击浏览器工具栏中的插件图标，再点击“模板编辑器”。

1. 点击“打开 ZIP 模板”。
2. 在只读流程图中点击节点。
3. 在右侧编辑文案、按钮文字、打开链接和素材变量。
4. 在“模板设置”中修改变量名称、说明、类型、默认值和预置选项。
5. 点击“导出当前模板”。

编辑器支持同时打开多个模板，顶部标签用于切换。导出按钮只导出当前选中的模板。

常见操作：

- 在文案或链接中输入 `[[变量名]]`，即可建立或复用文字变量。
- 多处使用相同的 `[[变量名]]`，导入时会统一填写和替换。
- 图片、音频或视频可设置相同的素材变量标识，实现多处共用。
- “随机选项”适合准备多套长文案，让插件在导入时随机选择。
- “高级 JSON”适合熟悉模板结构的用户；修改后必须先“验证并应用”。

条件、延迟和常见消息节点会按各自类型显示。尚未适配的特殊节点不会被错误当成文本节点，可在“节点 JSON”中查看和编辑。

模板中如果包含标签、自定义字段、商品、序列、其他 Flow 引用或复杂动作等专页绑定对象，插件会列出并阻止直接覆盖，避免把源专页 ID 错误带入目标专页。

## 九、备份和恢复

在 Flow 页面打开流程助手，点击右上角三点菜单，再进入“备份/恢复”。

- 每次替换流程前都会自动备份。
- 手动恢复前也会再次备份当前状态。
- 备份按“专页 ID + Flow ID”分别保存。
- 默认回复有自己独立的备份分组。
- 每个 Flow 最多保留 5 份，旧备份会自动清理。
- 本地空间不足时，插件会优先删除更旧的重复备份。

备份保存在当前浏览器的插件本地存储中。卸载插件、清除扩展数据或换电脑后，备份不会自动跟随。

流程备份主要保存节点结构和素材引用。如果素材后来被人在 Botcake 中手动删除，旧备份中的远程引用可能失效；需要长期迁移时，请另外保留导出的模板 ZIP，因为 ZIP 会尽量把素材一并打包。

## 十、常见问题

### 页面没有出现浮标

先点击一次浏览器工具栏中的插件图标。插件会尝试重新连接当前 Botcake 页面；仍未出现时，刷新 Botcake 页面。

### 控制台没有识别资源

请检查：

- Google 表格是否公开。
- 链接是否带 `gid`。
- 第一列名称是否以“设置”“流程”或“默认回复”开头。
- 第二列是否为可公开下载的文件链接，而不是文件夹链接。
- 修改表格后是否点击了“重新读取”。

### Google Drive 图片或 ZIP 无法下载

把文件共享权限改成“知道链接的任何人可查看”，再重新选择或应用。组织内部专用链接、需要登录或需要申请权限的链接无法免授权读取。

### 提示 Botcake 仍在切换专页或流程

等待页面加载完成后重试。插件会短暂自动重试，但 Botcake 本身加载较慢时仍可能需要重新打开助手。

### 提示 Botcake 接口 500

通常是 Botcake 服务暂时异常、页面状态尚未加载完成或当前账号权限不足。请稍后重试，并确认该账号能在 Botcake 页面中手动完成同一操作。

### 机器人变量显示“新建 0 个”

这不一定是错误。如果目标专页已有同名变量，插件会直接复用；已归档的变量会显示在“恢复归档变量”数量中。

### 应用后是否还要点击 Botcake 的 Update

插件通过 Botcake 的保存接口直接写入流程，成功提示后会自动刷新页面。建议首次使用某个新模板时再人工预览一次，确认按钮、素材和分支符合预期。

## 十一、推荐的新手工作方法

1. 先挑一个测试专页，把设置和流程手动配置正确。
2. 导出一份设置 JSON 和一份流程 ZIP。
3. 在模板编辑器中设置需要用户填写的变量和预置选项。
4. 把文件上传到公开网盘，并登记到控制台表格。
5. 先在另一个测试专页完整应用一次。
6. 确认评论回复、评论私信、欢迎信息、默认回复、机器人变量和素材都正常后，再用于正式专页。

这样后续的新手用户通常只需要：选择专页、应用设置、应用流程，两三次点击即可完成主要配置。
